module.exports = {
  firmWareInfo: function (request) {
    const { data } = request;

    const response = {
      result: 0,
    };
    return response;
  },

  LTSversion: function (request) {
    const { data } = request;

    const response = {
      result: 0,
    };
    return response;
  },
};
